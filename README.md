# breach-intelligence-analyzer
This  program sends password to an AI model along with instructions telling the model to act as a security auditor. The model sends back a written risk report and a score from 1 to 100
